# Руководство по управлению организациями

## Что было реализовано

Добавлена полноценная система управления организациями с разделением прав доступа для администраторов и владельцев организаций.

## Возможности

### Для суперадминистраторов (is_superuser)

1. **Просмотр всех организаций**
   - Видят список всех организаций в системе
   - Доступ к статистике (количество филиалов и пользователей)

2. **Управление организациями**
   - Создание новых организаций
   - Редактирование существующих
   - Удаление организаций

3. **Управление пользователями любой организации**
   - Просмотр пользователей
   - Создание новых пользователей
   - Редактирование пользователей
   - Удаление пользователей

### Для владельцев организаций (role='owner')

1. **Просмотр своей организации**
   - Видят только свою организацию

2. **Управление пользователями своей организации**
   - Просмотр пользователей организации
   - Создание новых пользователей
   - Редактирование пользователей
   - Удаление пользователей

## Бэкенд изменения

### Новые permissions

**`IsSuperAdmin`** - в `backend/apps/core/permissions.py`
- Проверяет, что пользователь является суперпользователем Django

### Обновленные endpoints

**Organizations API** (`/api/v1/org/organizations/`)
- `GET` - список организаций (для админа - все, для owner - своя)
- `POST` - создание организации (только админ)
- `PATCH/PUT` - обновление (только админ)
- `DELETE` - удаление (только админ)
- `GET /{id}/users/` - список пользователей организации
- `POST /{id}/users/` - создание пользователя в организации

**Organization Users API** (`/api/v1/org/organization-users/`)
- `GET` - список пользователей с фильтрацией по организации
- `POST` - создание пользователя
- `PATCH/PUT /{id}/` - обновление пользователя
- `DELETE /{id}/` - удаление пользователя

### Новые serializers

- `OrganizationSerializer` - с полями `branches_count` и `users_count`
- `OrganizationUserSerializer` - для отображения пользователей
- `OrganizationUserCreateSerializer` - для создания пользователей с валидацией паролей

## Фронтенд изменения

### Новые файлы

1. **`frontend/src/api/organizations.js`**
   - API клиент для работы с организациями

2. **`frontend/src/pages/OrganizationsPage.vue`**
   - Страница управления организациями
   - Таблица организаций с поиском
   - Модальные окна для создания/редактирования
   - Управление пользователями

### Обновленные файлы

1. **`frontend/src/router/index.js`**
   - Добавлен маршрут `/organizations`

2. **`frontend/src/layouts/MainLayout.vue`**
   - Добавлена ссылка "🏢 Организации" в меню (видна только админам и владельцам)

## Как использовать

### Для суперадминистратора

1. Войдите в систему с правами суперпользователя
2. В боковом меню появится пункт "🏢 Организации"
3. На странице организаций вы можете:
   - Создать новую организацию кнопкой "+ Новая организация"
   - Просмотреть список всех организаций
   - Редактировать организацию (кнопка ✏️)
   - Удалить организацию (кнопка 🗑️)
   - Управлять пользователями организации (кнопка "👥 Пользователи")

### Для владельца организации

1. Войдите в систему с ролью `owner`
2. В боковом меню появится пункт "🏢 Организации"
3. На странице вы увидите только свою организацию
4. Кнопка "👥 Пользователи" позволяет:
   - Просматривать пользователей вашей организации
   - Создавать новых пользователей
   - Редактировать существующих
   - Удалять пользователей

### Создание пользователя

При создании пользователя необходимо указать:
- **Имя пользователя** (username) - уникальное
- **Email**
- **Имя и Фамилия**
- **Телефон**
- **Роль** (owner, branch_admin, doctor, registrar, cashier, warehouse, marketer, readonly)
- **Пароль** (минимум 8 символов)
- **Подтверждение пароля**

### Роли пользователей

- **owner** - Владелец организации (может управлять пользователями)
- **branch_admin** - Администратор филиала
- **doctor** - Доктор
- **registrar** - Регистратор
- **cashier** - Кассир
- **warehouse** - Складской работник
- **marketer** - Маркетолог
- **readonly** - Только чтение

## API Примеры

### Получить все организации (админ)

```bash
GET /api/v1/org/organizations/
Authorization: Bearer <token>
```

### Создать организацию (админ)

```bash
POST /api/v1/org/organizations/
Authorization: Bearer <token>
Content-Type: application/json

{
  "name": "Новая клиника",
  "sms_sender": "Clinic"
}
```

### Получить пользователей организации

```bash
GET /api/v1/org/organizations/1/users/
Authorization: Bearer <token>
```

### Создать пользователя в организации

```bash
POST /api/v1/org/organizations/1/users/
Authorization: Bearer <token>
Content-Type: application/json

{
  "username": "newuser",
  "email": "user@example.com",
  "first_name": "Иван",
  "last_name": "Петров",
  "phone": "+7 777 123 45 67",
  "role": "doctor",
  "password": "SecurePass123",
  "password2": "SecurePass123"
}
```

## Безопасность

✅ Суперадминистраторы могут управлять всеми организациями
✅ Владельцы организаций видят только свою организацию
✅ Обычные пользователи не видят страницу организаций
✅ Пароли валидируются и безопасно хэшируются
✅ Нельзя удалить самого себя
✅ Права доступа проверяются на уровне бэкенда

## Первый запуск

1. Создайте суперпользователя Django:
```bash
cd backend
python manage.py createsuperuser
```

2. Войдите в систему с правами суперпользователя

3. Создайте организации через веб-интерфейс

4. Добавьте пользователей в организации

## Поддержка

Все функции полностью интегрированы и готовы к использованию. Если возникнут вопросы - пишите!

