# Быстрый старт: Управление перерывами

## ✅ Что готово

Функциональность управления перерывами сотрудников полностью реализована и протестирована!

## 🚀 Как использовать

### 1. Добавить перерыв

1. Откройте **Расписание** (Schedule Page)
2. Найдите нужного сотрудника в шапке колонок
3. Нажмите на **три точки** (⋮) рядом с именем сотрудника
4. Выберите **"☕ Добавить перерыв"**
5. В открывшемся окне:
   - Сотрудник будет выбран автоматически (можно изменить)
   - Выберите тип перерыва:
     - 🍽️ **Обед**
     - ☕ **Перерыв**
     - 👥 **Совещание**
     - 📝 **Другое**
   - Укажите время начала и окончания
   - Выберите дату
   - Добавьте примечание (опционально)
   - Если нужно, отметьте "Повторяющийся перерыв" (создастся на 30 дней вперед)
6. Нажмите **"Сохранить"**

### 2. Повторяющиеся перерывы

Если вы хотите создать регулярный перерыв (например, ежедневный обед с 12:00 до 13:00):

1. Создайте перерыв как обычно
2. **Установите галочку** "Повторяющийся перерыв (каждый день)"
3. Система автоматически создаст такой же перерыв на следующие 30 дней

### 3. Просмотр перерывов

Перерывы можно просмотреть:
- Через API: `GET /api/v1/calendar/breaks/`
- В Django Admin: раздел "Calendar" → "Breaks"

## 🔧 Технические детали

### API Endpoints

```javascript
// Получить перерывы
GET /api/v1/calendar/breaks/?employee=1&date_from=2025-11-05

// Создать перерыв
POST /api/v1/calendar/breaks/
{
  "employee": 1,
  "break_type": "lunch",
  "date": "2025-11-05",
  "start_time": "12:00:00",
  "end_time": "13:00:00",
  "note": "Обеденный перерыв",
  "is_recurring": true
}

// Удалить перерыв
DELETE /api/v1/calendar/breaks/5/

// Удалить все повторяющиеся перерывы
DELETE /api/v1/calendar/breaks/delete_recurring/?employee=1&start_time=12:00:00&end_time=13:00:00
```

### Примеры использования в коде

```javascript
import { createBreak, getBreaks } from '@/api/calendar'

// Создать перерыв
await createBreak({
  employee: 1,
  break_type: 'lunch',
  date: '2025-11-05',
  start_time: '12:00:00',
  end_time: '13:00:00',
  is_recurring: true
})

// Получить перерывы сотрудника
const response = await getBreaks({
  employee: 1,
  date_from: '2025-11-01',
  date_to: '2025-11-30'
})
```

## ✨ Что дальше?

В будущих версиях планируется:

1. **Визуальное отображение** перерывов в сетке расписания
2. **Редактирование** существующих перерывов
3. **Drag & drop** для изменения времени перерывов
4. **Цветовая кодировка** разных типов перерывов
5. **Уведомления** о приближающихся перерывах

## 🐛 Сообщить о проблеме

Если вы обнаружили ошибку или у вас есть предложение:
1. Проверьте логи backend: `docker-compose logs backend`
2. Проверьте консоль браузера (F12)
3. Создайте issue с описанием проблемы

## 📝 Примечания

- Перерывы проверяются на пересечения - нельзя создать два перерыва в одно время для одного сотрудника
- Время начала должно быть раньше времени окончания
- Повторяющиеся перерывы не создаются, если на дату уже существует перерыв с таким же временем

---

**Версия:** 1.0  
**Дата:** 05.11.2025

