# Исправление кассовой смены на странице Финансов

**Дата:** 5 ноября 2025  
**Страница:** `/billing`  
**Проблема:** Кассовая смена не открывается, новый платёж недоступен

## Описание проблемы

При попытке открыть кассовую смену на странице Финансов:
1. Смена не открывалась
2. Кнопка "Новый платёж" была доступна даже без открытой смены
3. Не было валидации на существующую открытую смену
4. Ошибки не отображались корректно

## Внесённые исправления

### Backend (`backend/apps/billing/`)

#### 1. **serializers.py** - Добавлена валидация смены
```python
def validate_branch(self, value):
    """Validate that no open shift exists for this branch"""
    existing_shift = CashShift.objects.filter(
        branch=value,
        closed_at__isnull=True
    ).first()
    
    if existing_shift:
        raise serializers.ValidationError(
            f'Кассовая смена для филиала "{value.name}" уже открыта. '
            f'Закройте текущую смену перед открытием новой.'
        )
    
    return value
```

**Эффект:** Теперь невозможно открыть две смены для одного филиала одновременно.

#### 2. **views.py** - Исправлен ответ API
```python
@action(detail=False, methods=['get'])
def current(self, request):
    # ...
    if shift:
        serializer = self.get_serializer(shift)
        return Response({'shift': serializer.data})  # Было: Response(serializer.data)
    else:
        return Response({'shift': None})
```

**Эффект:** API теперь всегда возвращает консистентный формат `{shift: ...}`.

#### 3. **views.py** - Добавлен импорт serializers
```python
from rest_framework import viewsets, status, serializers
```

### Frontend (`frontend/src/pages/BillingPage.vue`)

#### 1. **Блокировка кнопки "Новый платёж"**
```vue
<n-button 
  type="primary" 
  @click="showPaymentModal = true"
  :disabled="!currentShift"
  :title="!currentShift ? 'Откройте кассовую смену для создания платежей' : 'Создать новый платёж'"
>
  💰 Новый платёж
</n-button>
```

**Эффект:** Кнопка неактивна без открытой смены.

#### 2. **Улучшена обработка ошибок при открытии смены**
```javascript
const handleOpenShift = async () => {
  try {
    // ... validation ...
    
    const response = await billingApi.openCashShift({
      branch: shiftForm.value.branch,
      opening_balance: shiftForm.value.opening_balance
    })
    
    console.log('Shift opened successfully:', response.data)
    message.success('Кассовая смена успешно открыта')
    
    // Reset form
    shiftForm.value.opening_balance = 0
    
    // Reload current shift
    await loadCurrentShift()
  } catch (error) {
    // Detailed error handling with different error formats
    let errorMsg = 'Ошибка открытия смены'
    
    if (errorData.branch) {
      errorMsg = Array.isArray(errorData.branch) 
        ? errorData.branch[0] 
        : errorData.branch
    } else if (errorData.opening_balance) {
      errorMsg = Array.isArray(errorData.opening_balance) 
        ? errorData.opening_balance[0] 
        : errorData.opening_balance
    }
    // ... и другие форматы
    
    message.error(errorMsg, { duration: 5000 })
  }
}
```

**Эффект:** Подробные сообщения об ошибках для пользователя.

#### 3. **Исправлена загрузка текущей смены**
```javascript
const loadCurrentShift = async () => {
  // ...
  const response = await billingApi.getCurrentCashShift(shiftForm.value.branch)
  
  // API returns {shift: {...}} or {shift: null}
  currentShift.value = response.data.shift  // Было: response.data.shift || null
  // ...
}
```

**Эффект:** Корректная обработка ответа API.

#### 4. **Улучшен UI модального окна смены**
- Добавлено предупреждение при отсутствии филиалов
- Добавлена информация о текущей смене (включая название филиала)
- Добавлена подсказка о начальном остатке
- Кнопка открытия смены неактивна без доступных филиалов

## Проверка работы

1. Откройте страницу `/billing`
2. Убедитесь, что кнопка "Новый платёж" неактивна
3. Нажмите "🔒 Открыть смену"
4. Выберите филиал и укажите начальный остаток
5. Нажмите "Открыть смену"
6. Смена должна успешно открыться
7. Кнопка "Новый платёж" становится активной
8. При попытке открыть смену повторно, получите сообщение об ошибке

## Тестовые сценарии

### ✅ Успешное открытие смены
1. Нет открытых смен для филиала
2. Выбран корректный филиал
3. Указан начальный остаток ≥ 0
4. **Результат:** Смена открыта, кнопка "Новый платёж" активна

### ❌ Попытка открыть вторую смену
1. Смена уже открыта для филиала
2. Попытка открыть новую смену
3. **Результат:** Ошибка "Кассовая смена для филиала уже открыта"

### ✅ Создание платежа
1. Смена открыта
2. Кнопка "Новый платёж" активна
3. **Результат:** Можно создать платёж

### ❌ Создание платежа без смены
1. Смена не открыта
2. Кнопка "Новый платёж" неактивна
3. **Результат:** Невозможно создать платёж

## Изменённые файлы

### Backend
- `backend/apps/billing/serializers.py` - добавлена валидация
- `backend/apps/billing/views.py` - исправлен API, добавлен импорт

### Frontend
- `frontend/src/pages/BillingPage.vue` - улучшен UI и обработка ошибок

## Примечания

- Кассовая смена теперь корректно валидируется на уровне сервера
- Пользователь получает понятные сообщения об ошибках
- UI блокирует неправильные действия (создание платежа без смены)
- Каждый филиал может иметь только одну открытую смену одновременно

## Следующие шаги

1. ✅ Протестировать открытие смены
2. ✅ Протестировать создание платежа
3. ⏳ Протестировать закрытие смены
4. ⏳ Проверить отчёты по кассовым сменам

