# Исправление: Платежи и кассовая смена

## Дата: 5 ноября 2025

## Проблема
Пользователь не мог создать новый платеж, хотя кассовая смена была открыта. Система не проверяла наличие открытой смены корректно.

## Причины
1. **Frontend**: Кнопка "Новый платёж" была полностью заблокирована при отсутствии смены
2. **Backend**: Отсутствовала валидация наличия открытой смены при создании наличных платежей
3. **UX**: Не было четкой обратной связи пользователю о необходимости открыть смену

## Решение

### 1. Backend - Валидация кассовой смены (`backend/apps/billing/views.py`)

Добавлена проверка наличия открытой смены для наличных платежей:

```python
def perform_create(self, serializer):
    """Create payment with cash shift validation for cash payments"""
    payment_method = serializer.validated_data.get('method')
    
    # For cash payments, verify an open cash shift exists
    if payment_method == 'cash':
        invoice = serializer.validated_data.get('invoice')
        branch = invoice.visit.appointment.branch
        
        # Check if there's an open shift for this branch
        open_shift = CashShift.objects.filter(
            branch=branch,
            closed_at__isnull=True
        ).first()
        
        if not open_shift:
            raise serializers.ValidationError({
                'detail': 'Невозможно принять наличный платёж: кассовая смена не открыта. Откройте смену перед приёмом платежей.'
            })
    
    serializer.save()
```

**Важно**: Проверка применяется **только для наличных платежей** (`method='cash'`). Безналичные платежи (карта, Kaspi, Halyk Pay и т.д.) можно создавать без открытой смены.

### 2. Frontend - Улучшенный UX (`frontend/src/pages/BillingPage.vue`)

#### Изменения:

**a) Убрана блокировка кнопки "Новый платёж"**
```vue
<n-button 
  type="primary" 
  @click="showPaymentModal = true"
>
  💰 Новый платёж
</n-button>
```
Теперь пользователь может открыть форму платежа в любое время.

**b) Добавлено предупреждение в форме**
```vue
<n-alert 
  v-if="paymentForm.method === 'cash' && !currentShift" 
  type="warning" 
  style="margin-top: 8px"
  title="Внимание"
>
  Кассовая смена не открыта. Откройте смену для приёма наличных платежей.
</n-alert>
```
Предупреждение показывается **только когда выбран способ оплаты "Наличные"** и смена не открыта.

**c) Валидация перед отправкой**
```javascript
// Additional check for cash payments
if (paymentForm.value.method === 'cash' && !currentShift.value) {
  message.error('Невозможно принять наличный платёж: кассовая смена не открыта')
  return
}
```

**d) Улучшенная диагностика**
- Добавлены console.log для отладки загрузки смены
- Добавлена кнопка обновления статуса смены (🔄)
- Улучшенный визуальный индикатор (зеленая кнопка когда смена открыта)

**e) Улучшенная обработка ошибок**
```javascript
if (error?.response?.data) {
  const errorData = error.response.data
  const errorMsg = errorData.detail || JSON.stringify(errorData)
  message.error(`Ошибка: ${errorMsg}`)
}
```

## Как использовать

### Для наличных платежей:

1. **Откройте кассовую смену:**
   - Нажмите кнопку "🔒 Открыть смену" в верхнем правом углу страницы Финансы
   - Выберите филиал
   - Укажите начальную сумму в кассе
   - Нажмите "Открыть смену"

2. **Создайте платеж:**
   - Нажмите "💰 Новый платёж"
   - Введите ID счёта (Invoice)
   - Выберите "💵 Наличные"
   - Укажите сумму
   - Нажмите "Создать платёж"

3. **После окончания работы:**
   - Нажмите "🔓 Смена открыта"
   - Укажите сумму закрытия
   - Нажмите "Закрыть смену"

### Для безналичных платежей (карта, Kaspi, и т.д.):

Можно создавать **в любое время**, открытая смена не требуется:
1. Нажмите "💰 Новый платёж"
2. Выберите способ оплаты: Карта, Kaspi, Kaspi QR, Halyk Pay, Paybox или Cloud Payments
3. Заполните форму и создайте платёж

## Диагностика проблем

### Проблема: "Кассовая смена не открыта"

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте логи:
   ```
   Loading current shift for branch: X
   No open shift found for branch: X
   ```
3. Откройте кассовую смену через кнопку "🔒 Открыть смену"
4. Если смена уже открыта, нажмите кнопку обновления 🔄

### Проблема: Кнопка показывает "Смена открыта", но платеж не создается

**Решение:**
1. Нажмите кнопку обновления 🔄 рядом с "🔓 Смена открыта"
2. Проверьте в консоли:
   ```
   Current shift loaded: { id: ..., branch: ..., ... }
   ```
3. Если смены нет - откройте её заново
4. Проверьте, что филиал в смене совпадает с филиалом счёта

### Проблема: Backend возвращает ошибку 400

**Возможные причины:**
1. Счёт (Invoice) с указанным ID не существует
2. У счёта нет связанного визита или записи
3. Филиал недоступен для текущего пользователя

**Проверка:**
```python
# В Django shell
from apps.billing.models import Invoice, CashShift

# Проверить счёт
invoice = Invoice.objects.get(id=YOUR_INVOICE_ID)
print(invoice.visit.appointment.branch)

# Проверить открытые смены
shifts = CashShift.objects.filter(closed_at__isnull=True)
for shift in shifts:
    print(f"Shift {shift.id}: Branch {shift.branch.name}")
```

## Технические детали

### API Endpoints

**Проверка текущей смены:**
```
GET /api/v1/billing/cashshifts/current/?branch={branch_id}
```

**Открытие смены:**
```
POST /api/v1/billing/cashshifts/
{
  "branch": 1,
  "opening_balance": 0
}
```

**Создание платежа:**
```
POST /api/v1/billing/payments/
{
  "invoice": 123,
  "method": "cash",
  "amount": 5000.00,
  "provider": ""
}
```

### Бизнес-логика

1. **Наличные платежи** требуют открытую смену на филиале, к которому относится счёт
2. **Безналичные платежи** не требуют открытой смены
3. Смена привязана к **конкретному филиалу** и **кассиру**
4. Одновременно может быть открыта только **одна смена на филиал**

## Изменённые файлы

1. `backend/apps/billing/views.py` - добавлена валидация смены в `PaymentViewSet.perform_create()`
2. `frontend/src/pages/BillingPage.vue` - улучшен UX и добавлены проверки

## Тестирование

### Тест 1: Создание наличного платежа без смены
1. Убедитесь, что смена закрыта
2. Попытайтесь создать наличный платёж
3. **Ожидается**: Предупреждение + ошибка

### Тест 2: Создание безналичного платежа без смены
1. Убедитесь, что смена закрыта
2. Создайте платёж картой/Kaspi
3. **Ожидается**: Платёж успешно создан

### Тест 3: Создание наличного платежа с открытой сменой
1. Откройте смену
2. Создайте наличный платёж
3. **Ожидается**: Платёж успешно создан

## Совместимость

- ✅ Django 4.2+
- ✅ Vue 3
- ✅ Naive UI
- ✅ Все современные браузеры

## Миграции

Не требуются. Изменения только в логике, схема БД не изменилась.



