# 🚀 Быстрый старт Telegram Bot

## За 3 шага

### 1️⃣ Получите токен бота

1. Откройте Telegram и найдите **[@BotFather](https://t.me/BotFather)**
2. Отправьте команду `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2️⃣ Настройте .env файл

Откройте файл `.env` в корне проекта и найдите эти строки:

```env
# Telegram Bot Settings
TELEGRAM_BOT_TOKEN=        # ← ВСТАВЬТЕ ВАШ ТОКЕН ЗДЕСЬ
TELEGRAM_BOT_API_SECRET=change-this-secret-in-production  # ← Придумайте сложный секрет
```

**Пример:**
```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_BOT_API_SECRET=my-super-secret-key-12345
```

### 3️⃣ Запустите бота

**Вариант A: Автоматический запуск (рекомендуется)**

```powershell
# Windows PowerShell
.\start_bot.ps1
```

**Вариант B: Ручной запуск**

```bash
# Запуск бота
docker-compose up -d telegram_bot

# Просмотр логов
docker-compose logs -f telegram_bot
```

## ✅ Проверка

1. Найдите вашего бота в Telegram по username
2. Отправьте `/start`
3. Бот должен ответить приветственным сообщением 👋

## 🔧 Управление ботом

```bash
# Остановить бота
docker-compose stop telegram_bot

# Запустить заново
docker-compose start telegram_bot

# Перезапустить (после изменений в коде)
docker-compose restart telegram_bot

# Пересобрать образ (после изменений в Dockerfile или requirements.txt)
docker-compose build telegram_bot
docker-compose up -d telegram_bot

# Посмотреть статус
docker-compose ps telegram_bot

# Логи последних 100 строк
docker-compose logs --tail=100 telegram_bot

# Следить за логами в реальном времени
docker-compose logs -f telegram_bot
```

## 📋 Что использует бот?

### Зависимости (автоматически запускаются):
- ✅ **Backend** (`http://backend:8000`) - Django API
- ✅ **Redis** - для хранения состояний FSM
- ✅ **PostgreSQL** - база данных (через backend)

### Endpoints:
Бот взаимодействует с Django API через `/api/bot/`:
- Регистрация и управление пациентами
- Запись на приём
- Документы и оплата
- Обратная связь и поддержка

## 🐛 Проблемы?

### Бот не отвечает

**1. Проверьте, что бот запущен:**
```bash
docker-compose ps telegram_bot
```

**2. Посмотрите логи:**
```bash
docker-compose logs telegram_bot
```

**3. Проверьте токен:**
- Откройте `.env`
- Убедитесь, что `TELEGRAM_BOT_TOKEN` правильный
- Токен должен быть от @BotFather

**4. Проверьте backend:**
```bash
docker-compose ps backend
```

### Backend недоступен

**1. Запустите backend:**
```bash
docker-compose up -d backend
```

**2. Проверьте, что backend работает:**
```bash
curl http://localhost:8000/api/bot/support/faq/
```

### Ошибка "Redis connection refused"

**1. Запустите Redis:**
```bash
docker-compose up -d redis
```

**2. Проверьте подключение:**
```bash
docker-compose exec redis redis-cli ping
```

## 🔐 Безопасность

### Production Checklist:

- [ ] Измените `TELEGRAM_BOT_API_SECRET` на сложный случайный ключ
- [ ] Измените `TELEGRAM_WEBHOOK_SECRET` 
- [ ] Установите `USE_WEBHOOK=true` для production
- [ ] Настройте HTTPS для webhook
- [ ] Не коммитьте `.env` файл в git

**Генерация секретов:**
```bash
# Python
python -c "import secrets; print(secrets.token_urlsafe(32))"

# PowerShell
-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
```

## 📚 Дополнительная документация

- [Полная документация бота](telegram_bot/README.md)
- [API Endpoints](docs/telegram-bot.md)
- [Архитектура](docs/architecture.md)

## 🎯 Следующие шаги

1. **Настройте команды бота** через @BotFather:
   ```
   start - Начать работу с ботом
   booking - Записаться на приём
   my_appointments - Мои записи
   documents - Мои документы
   support - Техподдержка
   ```

2. **Настройте организацию** в Django Admin:
   - Создайте филиалы
   - Добавьте услуги
   - Настройте расписание врачей

3. **Протестируйте функции**:
   - Регистрация пациента
   - Запись на приём
   - Получение документов

---

**Готово! 🎉** Ваш Telegram бот настроен и готов к работе!

Вопросы? Проверьте [FAQ](telegram_bot/README.md#-troubleshooting) или создайте issue.

