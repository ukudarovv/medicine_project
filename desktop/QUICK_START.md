# 🚀 Быстрый старт - Medicine Desktop App

## 📋 Требования

- ✅ Node.js 18+ (установлен в `C:\Program Files\nodejs\`)
- ✅ Backend запущен на `http://localhost:18000`
- ✅ Telegram Bot работает для OTP

## 🎯 Запуск приложения

### Вариант 1: PowerShell (рекомендуется)
```powershell
cd desktop
.\start_desktop.ps1
```

### Вариант 2: Batch файл
```batch
cd desktop
start_desktop.bat
```

## 🔐 Данные для входа

```
Логин:  doctor
Пароль: doctor123
```

## 🏥 Как использовать

### 1. Авторизация
- Откроется окно Electron с формой входа
- Введите логин: `doctor`
- Введите пароль: `doctor123`
- Нажмите "Войти"

### 2. Запрос доступа к пациенту
- Введите **ИИН пациента** (12 цифр)
- Укажите **причину запроса** (например: "Плановый осмотр")
- Выберите необходимые права доступа:
  - ✓ Чтение краткой информации
  - ✓ Чтение медицинских записей  
  - ✓ Создание медицинских записей
- Нажмите "Запросить доступ"

### 3. Ожидание подтверждения
- На телефон пациента отправится запрос через Telegram
- Приложение автоматически проверяет статус каждые 3 секунды
- Когда пациент подтвердит - откроется форма диктовки

### 4. Диктовка (в разработке)
- После подтверждения доступа загрузятся данные пациента
- Можно вводить текст медицинской записи вручную
- Голосовая диктовка в разработке

### 5. Сохранение
- Проверьте текст
- Нажмите "Отправить в ERP"
- Запись сохранится в системе

## ⚙️ Настройки

Файл `.env`:
```env
VITE_API_URL=http://localhost:18000/api/v1
```

## 🐛 Устранение проблем

### Network Error при авторизации
**Причина:** Приложение не может подключиться к backend API

**Решение:**
1. Проверьте, что backend запущен:
   ```powershell
   docker ps | findstr backend
   ```
2. Проверьте доступность API:
   ```powershell
   curl http://localhost:18000/api/v1/auth/login/
   ```
3. Перезапустите десктоп-приложение

### Electron не запускается
**Причина:** Dev server еще не готов

**Решение:**
- Подождите 10-15 секунд после запуска
- Проверьте логи в консоли PowerShell

### Ошибка "Python required"
**Причина:** Установка native модулей требует Python

**Решение:**
- Уже исправлено - удалены проблемные модули из `package.json`
- Если проблема повторяется, удалите `node_modules` и запустите заново

## 📊 Порты

- **Frontend (Vite):** http://localhost:5174
- **Backend API:** http://localhost:18000
- **Main Backend:** http://localhost:8000  
- **WebSocket:** ws://localhost:8001

## 🔧 Разработка

### Просмотр логов
Логи отображаются в:
- PowerShell/CMD окне (npm output)
- DevTools в Electron (Console)

### Горячая перезагрузка
- React код - автоматически (HMR)
- Electron main process - требует перезапуска

### Структура проекта
```
desktop/
├── electron/         # Main process
│   ├── main.js      # Главный файл Electron
│   └── preload.js   # Preload скрипт
├── src/             # React app (renderer)
│   ├── pages/       # Страницы
│   ├── components/  # Компоненты
│   ├── services/    # API клиент
│   └── store/       # State management (Zustand)
├── .env             # Конфигурация
└── package.json     # Зависимости
```

## 📝 Примечания

- Офлайн-режим в разработке
- Голосовая диктовка (Vosk) в разработке
- Voice Activity Detection в разработке

## 🆘 Поддержка

При проблемах проверьте:
1. Backend запущен и доступен
2. Telegram Bot работает
3. Node.js установлен и в PATH
4. Файл `.env` существует и настроен правильно

---

**Version:** 1.0.0  
**Last Updated:** 2025-11-05

